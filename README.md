# rustyshell

Customized shell game.

## Build

```shell
cargo build --release
```

## Usage

The file `game_config.toml` located in `./tests_set` defines the game process. You can modify the game by editing this file.